<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="teleconsultaPU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        
        <class>br.com.teleconsulta.model.Usuario</class>
        <class>br.com.teleconsulta.model.Paciente</class>
        <class>br.com.teleconsulta.model.UnidadeSaude</class>
        <class>br.com.teleconsulta.model.Sala</class>
        <class>br.com.teleconsulta.model.Reserva</class>

        <properties>
            <!-- Configuração H2 em memória (funciona sempre) -->
            <property name="javax.persistence.jdbc.driver" value="org.h2.Driver"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:h2:file:/tmp/teleconsulta;AUTO_SERVER=TRUE"/>
            <property name="javax.persistence.jdbc.user" value="sa"/>
            <property name="javax.persistence.jdbc.password" value=""/>

            <!-- Hibernate Dialect -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>

            <!-- Criar schema automaticamente -->
            <property name="hibernate.hbm2ddl.auto" value="create-drop"/>

            <!-- Exibir SQL -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>

            <!-- Pool de conexões -->
            <property name="hibernate.connection.pool_size" value="10"/>

            <!-- Cache -->
            <property name="hibernate.cache.use_second_level_cache" value="false"/>
            <property name="hibernate.cache.use_query_cache" value="false"/>
        </properties>
    </persistence-unit>
</persistence>
